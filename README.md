
Steps to run:
1) Backend
   cd backend
   npm install
   create .env file (see backend/.env.example)
   npm start

2) Frontend
   cd frontend
   npm install
   npm start

Frontend runs on http://localhost:3000 and calls backend at http://localhost:4000
